//设置参数 Setting Parameter


//端口号
ComNum = com1;

//比特率
BaudRate = 9600;

//数据位
ByteSize = 8;

//停止位-ONESTOPBIT(0),ONE5STOPBITS(1),TWOSTOPBITS(2)
StopBits = 1;

//校验位-NOPARITY(无0),ODDPARITY(奇1),EVENPARITY(偶2),MARKPARITY(3),SPACEPARITY(4)
Parity = 0;

//启动延时，单位秒
TestDelay = 10000;
